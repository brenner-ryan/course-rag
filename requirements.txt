chromadb==1.5.9
fastapi==0.141.1
uvicorn==0.52.4

# testing only
pytest==9.1.1
httpx==0.28.1

# Note: there is no machine-learning dependency here on purpose.
# The local language model runs on onnxruntime, and onnxruntime, tokenizers, numpy and
# huggingface_hub all arrive as ChromaDB dependencies because Chroma uses them for
# embeddings. The first version of this project used transformers and torch instead,
# which added about 990 MB to run a model with 80 million parameters.
